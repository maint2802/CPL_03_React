import React, { useState } from "react";

const Footer = () => {
  return (
    <div>
      <h2>Footer</h2>
    </div>
  );
};

export default React.memo(Footer);
// HOC

// useEffect
// bootstrap
// react.memo
